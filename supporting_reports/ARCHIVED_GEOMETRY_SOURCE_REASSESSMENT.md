# Archived geometry controls under the longitudinal source gate

Date: 2026-09-09

## Result

The thirteen transfers preserve the sampled packet timelikeness and leave the
small-coefficient longitudinal source route excluded. At
`k/R0_reference² = 0.01`, increasing the radial core gain from 0.05 to 0.06
raises the opening supply-to-demand ratio by 0.315% over `[-7,7]`. The
support-radius increases lower that ratio. No archived transfer changes an
excluded reference gate into an undecided one at the same quantum strength,
interval, and clock allowance.

The comparison therefore reaches its stated stopping point. The retained
service-oriented controls provide incremental static opening changes on this
phase; the source gap remains substantially larger. The result applies to the
tested parameter transfers and longitudinal channel law. The complete
Einstein--material--quantum construction remains open.

## Bounded comparison

This investigation applies the newer integrated opening requirement to the
geometry alternatives recovered by the
[historical cross-reference](GEOMETRY_DESIGN_HISTORY_CROSS_REFERENCE.md).
The current repaired beta075 background is the common reference. Archived
parameter changes are transferred individually onto it, retaining the other
component controls. This tests their effect in the current construction;
the original Stage I and Stage II ledgers remain the evidence for their
historical embodiments.

The [candidate specification](../toolkit/adm_harness_cli/specs/archived_geometry_opening.json)
contains thirteen cases: the reference; support radii 1.90 and 2.05; angular
widths 1.80 and 2.20; radial core gains 0.04 and 0.06; the previous and broader
catch rings; the ring-only comparison with the later skirt removed; and three
receiver/beta controls. The latter verify the difference between a control's
service role and its activity on the selected static phase.

The phase is 0.745, matching the retained quantum-construction geometry.
The metric has the existing C2 receiver, origin, and shell repairs. The test
uses that metric directly through its tabulated lapse, radial scale, and
areal radius. The saved condensate exterior and material solution remain
specific to their original background. The present necessary gate instead
grants every remaining component an arbitrary aggregate tensor whose radial
null projection is nonnegative.

## Source and service conditions

The source is the same ideal longitudinal conformal channel law used in the
[coupled reorientation](COUPLED_REORIENTATION_INVESTIGATION.md), with constant
total central charge along each tested interval. Gravitational normalization
and central charge are held fixed across candidates. The eleven strengths
are one central-charge unit and the ten coefficients corresponding to
`k/R0_reference² = 1e-6, 1e-5, 1e-4, .001, .01, .03, .1, .3, .6, .9`.
Each candidate's own `k/R0²` is also recorded.

Intervals are `[-3,3]`, `[-5,5]`, and `[-7,7]` in the existing radial
coordinate. The shorter intervals deliberately grant a shorter optical path.
Any channel spanning a tested interval has at least its optical length;
setting the exterior return to zero therefore gives optimistic supply.
Clock variations receive independent pointwise boxes of 0%, 10%, and 50%,
with the interval's endpoint jets retained. Their upper bound grants the
largest numerator and shortest optical length separately. The clock boxes
are exclusion tests whose service admissibility remains open.

The scalar service check preserves the selected live start, restored arrival,
V5 schedule, and packet radius. It samples 65 times and 41 packet offsets,
recomputes the packet norm with the repaired metric, and records the two
centerline transport proxies. A non-timelike sample rejects a transfer at this
level. A clean scalar screen leaves the established full carrier, source,
quiet-packet, and component-exchange requirements for any promoted candidate.

## Independent form of the opening identity

For `dl = B dx`, `a = log A`, and
`W = exp[-(R²-R0²)/(2k)]`, the required integrated opening is

\[
D=\int W\frac{R R''}{k}\,dl+[Wa']
 =\int W\left(\frac{R^2}{k^2}-\frac1k\right)(R')^2\,dl
  +\left[W\left(a'+\frac{RR'}k\right)\right].
\]

Primes denote proper derivatives and brackets retain both endpoints. The
available Casimir contribution is

\[
S=\frac{4\pi^2}{L_{\rm opt}^2}\int\frac{W}{A^2}\,dl,
\qquad S-D=\frac{4\pi}{k}\int W R^2 H_{\rm rem}\,dl.
\]

For the tested coefficients below the minimum radius squared, the bulk term
in the first-derivative form is nonnegative. It also avoids cancellation
between positive and negative radius second derivatives. The calculation
compares this expression with the original curvature form and with a direct
Einstein-minus-quantum null integral.

Composite Gaussian quadrature integrates in the native coordinate with the
proper measure B. It includes a focused throat mesh for the smallest quantum
coefficient. The registered comparison uses 8,193 and 16,385 metric samples,
and eight- versus sixteen-node Gaussian quadrature. A relative discrepancy
above 0.001 stops the corresponding calculation for refinement. Reported
exclusions retain a margin larger than the measured sampling difference.

The static tensor summary separately records negative energy, negative radial
null stress, and absolute angular pressure with proper-volume weighting.
These quantities describe demanded stress on each candidate. They retain
the distinction between source allocation and supplied material/quantum stress.

## Resource and continuation limits

The computation uses four processes, single-thread numerical libraries, a
900-second calculation allowance, and a 20 MB output allowance. Metric samples
and scalar tables provide the retained evidence. A complete spacetime ledger
and an absolute quantum mode sum become relevant only after a candidate
survives the necessary source gate and its applicable service checks.

The comparison first asks whether the archived controls materially improve
the fixed-strength opening ratio or remove an existing exclusion. If the
small-coefficient route remains excluded even with the generous clock box,
and the retained alternatives provide only incremental changes, this round
ends at that barrier. Larger coefficients remain explicitly recorded so that
the result distinguishes geometry relief from added quantum-source strength.

## Fixed-strength results

The reference coefficient `k/R0_reference² = 0.01` corresponds to total central
charge about 64,997 under the existing gravitational normalization. The table
uses `[-7,7]` and zero exterior return. Percentages are fractions of the
required integrated balance supplied by the quantum term.

| Transferred control | Original clock: supplied percentage | Optimistic 50% clock-box ceiling | Ratio change relative to reference |
| --- | ---: | ---: | ---: |
| Current reference | 0.461481% | 4.15333% | reference |
| Support radius 1.90 | 0.440499% | 3.96450% | -4.55% |
| Support radius 2.05 | 0.422471% | 3.80224% | -8.45% |
| Angular width 1.80 | 0.460940% | 4.14846% | -0.117% |
| Angular width 2.20 | 0.460584% | 4.14525% | -0.194% |
| Radial core gain 0.04 | 0.460032% | 4.14029% | -0.314% |
| Radial core gain 0.06 | 0.462933% | 4.16640% | +0.315% |
| Previous ring geometry | 0.461248% | 4.15123% | -0.0505% |
| Broader ring | 0.461441% | 4.15297% | -0.00865% |
| Outer skirt removed | 0.461401% | 4.15261% | -0.0172% |

Both receiver controls and the w8/t2 beta control reproduce the reference's
static metric exactly at all 16,385 samples. The receiver memory driver is
zero at phase 0.745. The beta collar changes the active carrying flow while
leaving the static lapse and spatial metric unchanged. The ring, skirt, and
core remain useful service components; their static influence at this phase
is small and spatially distinct from their main handoff duty.

Shorter optical paths increase the available negative quantum contribution.
For the strongest geometry improvement at this coefficient, the 0.06 core
gain, the optimistic clock-box ceilings are 17.1476% on `[-3,3]`, 7.47541%
on `[-5,5]`, and 4.16640% on `[-7,7]`. All remain below the necessary balance.

Across the 1,287 combinations of geometry, coefficient, interval, and clock
box, 1,133 are excluded and 154 remain undecided by this necessary bound.
All 819 cases through `k/R0_reference² = 0.03` are excluded. Within that
coefficient range, the largest relative geometry gain is 1.53%, for angular
width 2.20 at central charge one; its absolute supply remains very small.
For the reference at central charge one on `[-7,7]`, `S/D = 2.13403e-7`.

The stronger-coefficient results retain a useful scope distinction. At
`k/R0_reference² = 0.9`, about 5.85 million central-charge units, the original
clock passes the local `[-3,3]` gate with ratio 1.18350 for the reference and
1.63076 for radius 2.05. This is a 37.8% improvement in that short-interval
comparison, where the reference already passed. Every original-clock case on
`[-5,5]` and `[-7,7]` still fails across the registered coefficient ladder.
The local positive result supplies neither the longer optical loop nor a
complete supporting field construction.

## Why radius broadening loses the source comparison

Increasing support radius lowers the pointwise negative-density and negative
radial-null peaks. It also spreads the source demand over a larger proper
volume and changes the clock and optical path together. At radius 2.05,
the optical length over `[-7,7]` grows from 15.77995 to 16.23291, while the
throat lapse grows from 81.66196 to 88.46736 in the fixed exterior
normalization. At the reference coefficient 0.01 the weighted demand falls
from 0.282221 to 0.265429, but the available quantum term falls faster.

The separately counted static volume integrals show the same tradeoff:

| Static demanded-stress integral over `[-7,7]` | Reference | Radius 2.05 | Change |
| --- | ---: | ---: | ---: |
| Negative-density magnitude | 3.96427 | 4.58916 | +15.8% |
| Negative radial-null magnitude | 9.47473 | 10.53566 | +11.2% |
| Absolute angular pressure | 24.86253 | 31.66752 | +27.4% |

The measure is `4 pi R² dl`. These are source-demand diagnostics, distinct
from ADM mass and from a supplied material allocation. They reproduce the
historical lesson that a smaller local peak can accompany a larger counted
support requirement. Negative demanded energy persists in every candidate,
so the existing independently held planar-cell energy obstruction also
persists for an assembly whose other complete components have nonnegative
energy density.

## Service screen and verification

Each candidate has 2,629 live samples after the packet-mask check, with zero
non-timelike samples. Prepared duration remains 3.045. The weakest normalized
packet margin is the wider beta control, with maximum `norm/A² = -0.100515`.
The other cases reach maxima between -0.406708 and -0.374884. The coarse
packet-coordinate transport proxy changes from 1.23682 for the reference to
1.20661 for radius 2.05. Matching the original exterior endpoints and service
rating would require the corresponding trajectory comparison. The necessary
source exclusion already applies while granting these transfers their sampled
packet safety.

The reference metric samples reproduce the retained quantum-geometry cache
exactly. Maximum changes under doubled metric sampling are `7.72e-8` for the
gate quantities; doubled Gaussian order changes them by at most `9.92e-11`.
The first-derivative, curvature, and Einstein/CFT identities agree within
`6.60e-11` in the scaled norm.

The [independent curvature audit](../toolkit/adm_harness_cli/scripts/audit_archived_geometry_opening.py)
uses the original four-dimensional metric/connection calculation at ten
witnesses on the reference and radius-2.05 transfer, with three finite-
difference stencil sizes. Its finest tensor comparison agrees within
`1.75e-5` in the full-tensor normalized norm; the static current is zero.
The pointwise comparison retains the interpolation error of the tabulated
metric's second derivatives. All eleven geometry-opening and longitudinal-
balance tests pass, including an analytic proper-coordinate comparison,
time-normalization covariance, resolved small coefficients, and retained
endpoint terms.

The four-worker comparison took 163.4 seconds. The largest recorded worker
resident set was 225,440 KiB, about 220 MiB. Its metric caches, gate table,
summary, and independent audit occupy about 5.0 MB. Numerical evidence is
[summary.json](data/archived_geometry_opening/summary.json),
[gates.csv](data/archived_geometry_opening/gates.csv), and
[curvature_audit.csv](data/archived_geometry_opening/curvature_audit.csv).
The comparison setup is committed as `d9b93df`; this report is manually written.

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 PYTHONPATH=toolkit/adm_harness_cli python toolkit/adm_harness_cli/scripts/screen_archived_geometry_opening.py --workers 4 --output supporting_reports/data/archived_geometry_opening
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 PYTHONPATH=toolkit/adm_harness_cli python toolkit/adm_harness_cli/scripts/audit_archived_geometry_opening.py --workers 2
PYTHONPATH=toolkit/adm_harness_cli python -m pytest -q toolkit/adm_harness_cli/tests/test_geometry_opening.py toolkit/adm_harness_cli/tests/test_longitudinal_balance.py
```

The runner requires an empty output directory. The retained evidence is the
completed run; a replay uses a separate directory with the same specification.
