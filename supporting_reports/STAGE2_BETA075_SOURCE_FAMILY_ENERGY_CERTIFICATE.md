# Stage II Beta075 Source-Family Energy Certificate

## Status

Status: `fixed_background_energy_estimate_watch_pass`.

The formal regulated-director/support-reservoir source family now has a
fixed-background symmetric-hyperbolic / energy-estimate certificate over the
available complete surfaces. The certificate uses the validated block-symmetric
principal matrix and an identity block symmetrizer. It carries the support
exchange and scheduled source response as explicit lower-order work constants,
rather than hiding them in the principal part.

The hard energy certificate passes. There are no failed gates and no active
point violates the configured energy hard bounds. The result is still
watch-bounded: the dense flux margin is thin, the lower-order work constant is
large, the local exchange shape remains sharp, the support closure ratio is
near gate, and the phase-local source response remains severe.

## Machine Output

Structured output:

`toolkit/adm_harness_cli/runs/stage2_beta075_source_family_energy_certificate/`

Rows:

- theorem assumptions: `5`
- energy point rows: `63906`
- run summary rows: `3`
- local summary rows: `24`
- gate rows: `10`
- decision rows: `1`

No report text is generated by the harness.

## Theorem Skeleton

The certificate is a fixed-background theorem-style statement under five
assumptions:

1. the beta075 service metric is prescribed;
2. heat current is carried by bounded rapidity `v_q = tanh(psi)`;
3. the support reservoir is a derivative storage/stress operator;
4. source response is phase-local and service-scheduled;
5. constants are certified only on sealed baseline V5, sealed dense V5, and
   lower-service dense V2.

The energy structure uses:

```text
E[U] = 1/2 int U^T H U dV
H = I on (h, psi, chi_Omega, pi_Omega, Phi_support, Pi_support, n_l)
```

For the formal block principal matrix, `H A^l` is symmetric to numerical
precision. The support exchange terms `P` and `F` enter as controlled
lower-order work, together with the total support-closure constant and the
scheduled phase-local source bound.

## Result

Decision:

```text
energy_certificate_status: fixed_background_energy_estimate_watch_pass
hard_energy_certificate_pass: true
failed_gate_count: 0
watch_count: 5
surface_count: 3
```

Run summary:

| surface | status | fail rows | watch rows | min flux margin | max work constant | support work | local exchange | closure ratio |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| sealed baseline V5 | watch | `0` | `74` | `2.407e-04` | `1.349510` | `0.754089` | `0.327001` | `0.302640` |
| sealed dense V5 | watch | `0` | `28359` | `7.881e-05` | `2.049051` | `0.898931` | `0.609031` | `0.544567` |
| lower-service dense V2 | watch | `0` | `28359` | `7.881e-05` | `2.049050` | `0.898923` | `0.609031` | `0.544567` |

Gate summary:

| gate | status | value | read |
| --- | --- | ---: | --- |
| cross-surface hard validation | pass | watch-pass | inherits hard-clean surfaces |
| positive symmetrizer | pass | `1.0` | identity symmetrizer is positive definite |
| principal symmetry | pass | `0` | symmetrized principal matrix is symmetric |
| energy flux inside cone | watch | `7.881e-05` | inside service cone, thin |
| lower-order work bound | watch | `2.049051 / 2.5` | bounded but large |
| local exchange shape | watch | `0.609031 / 0.8` | bounded but sharp |
| support closure work | watch | `0.544567 / 0.55` | below gate, close |
| scheduled source bound | watch | `0.092617` | bounded source response, severe scaling |
| point failures | pass | `0` | no active energy hard-gate failures |
| available surface scope | pass | `3` | limited to generated complete surfaces |

## Interpretation

This is the strongest fixed-background PDE statement so far. The source-family
equations are not just algebraically compatible and not merely empirically
stable on the tested surfaces. They admit a symmetric energy estimate under
explicit assumptions and constants.

The certificate is also honest about what it does not prove. The work constant
is high: sealed dense V5 reaches `2.049051`, driven by support exchange,
local `P/F` shape, and near-gate support closure. The flux margin is still the
same thin `7.881e-05`. This is enough for a watch-pass energy estimate, not a
wide-margin theorem.

The lower-service dense V2 surface does not create a new problem. It lands at
essentially the same work and flux constants as sealed dense V5, slightly
cleaner in the last digits. That reinforces the previous cross-surface read:
the watch debt is stable across the available complete surfaces.

## Claim Boundary

Included:

- fixed-background symmetric energy estimate for the formal source-family PDE;
- positive block symmetrizer;
- in-cone energy flux bound;
- explicit lower-order support/source work constants;
- available-surface theorem assumptions.

Excluded:

- final matter action;
- global hyperbolicity theorem;
- arbitrary temporal forcing theorem;
- broad beta/V robustness beyond generated complete surfaces;
- coupled Einstein-matter evolution.

## Current Read

The academic narrative can now say:

```text
On the tested fixed-background surfaces, the regulated
director/support-reservoir source family is a symmetric-hyperbolic
watch-pass system with a positive energy norm, in-cone flux, controlled
lower-order support work, and bounded scheduled source response.
```

The unresolved debt is no longer physical viability. It is theorem strength
and margin breadth.

## Next Rung

The next rigorous gate is first-order coupled consistency / constraint
propagation for the total endpoint-plus-support source. The question is whether
the fixed-background source family can be inserted into the Einstein constraint
and Bianchi-accounting story at perturbative order without creating an
uncontrolled metric backreaction, constraint-growth mode, or non-conserved
total stress.

That should remain first-order and diagnostic. It should not claim full
coupled Einstein-matter evolution unless the constraint-propagation gate earns
that step.
